// export function from<T>(iterable: Iterable<T> | ArrayLike<T>): T[] {
//   return Array.from(iterable || []);
// }

// export function ofNumbers() {
//   return this.map(x => Number(x?.toString().trim() || undefined)).filter(x => Number.isInteger(x));
// }
//
